// Placeholder adaptive service
// Logic will be expanded later

export const getNextDifficulty = async (currentLevel, performance) => {
  return currentLevel;
};

export const updateLearningState = async (userId, result) => {
  return {
    userId,
    updated: true
  };
};
